version: '3.4'
services:

  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=mypass
    networks:
      - default

networks:
  default:
    driver: bridge

